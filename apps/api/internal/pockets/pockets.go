// Package pockets is scaffolded but intentionally empty — fill in as you
// build this feature. Keep its DB access behind a Repository type in this
// package, the same pattern used in internal/ledger, so other modules
// never reach into pockets's tables directly.
package pockets
